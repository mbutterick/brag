#lang info

(define collection 'multi)

(define deps '("brag-lib"))

(define build-deps '("at-exp-lib"
                     "br-parser-tools-doc"
                     "racket-doc"
                     "scribble-lib"))

(define implies deps)